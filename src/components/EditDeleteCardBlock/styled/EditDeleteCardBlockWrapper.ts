import styled from "styled-components/macro";

export const EditDeleteCardBlockWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;

  position: absolute;
  right: 0;
  top: 0;

  padding: 24px;
`;
